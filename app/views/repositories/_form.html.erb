<p>
  <label for="repository_name">Name:</label>
  <%= form.text_field :name %>
</p>
<p>
  <label for="repository_path">Path:</label>
  <%= form.text_field :path %>
</p>
<p>
  <label for="repository_subdomain">Subdomain:</label>
  <%= form.text_field :subdomain %>
</p>

<p style="border-bottom:0">
  <label>Public:</label> 
  <%= form.check_box :public %> Yes, the public can browse this repository.
</p>

<% if form.object && !form.object.new_record? && form.object.sync? -%>
<p>You have some revisions to sync: <%= form.object.revisions_to_sync.inspect %></p>
<% end -%>
